# Streaming analytics tutorial (real time)

This tutorial builds on `../basic/` and demonstrates two things:

1) how to **collect** streaming events into an append-only dataset (NDJSON)
2) how to run a simple **real-time analysis** loop (windowed stats + throughput)

Start here:

- `tutorial.md`

